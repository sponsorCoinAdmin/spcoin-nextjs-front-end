<ExchangeProvider>
  └── <PriceView>
        ├── useSwapTokensEffect()
        ├── usePriceErrorEffect()
        ├── useResetAmountsOnTokenChange()
        │     └── <TokenSelectContextView>
        │           └── <MainTradingPanel>
        │                 ├── <TradeContainerHeader>
        │                 ├── <TokenSelectSection>
        │                 │     ├── <AssetSelectProvider> (SELL)
        │                 │     │     └── <BaseSelectPanel> (SELL)
        │                 │     │           ├── <input /> ← user edits amount
        │                 │     │           ├── <TokenSelectDropDown>
        │                 │     │           │     └── onClick → setSpCoinDisplay(TOKEN_LIST_SELECT_PANEL)
        │                 │     │           ├── isContainerVisible → <TokenSelectScrollPanel>
        │                 │     │           │     └── <TokenListSelectPanel>
        │                 │     │           │           └── <AssetListSelectPanel>
        │                 │     │           │                 └── <AddressSelect> (SELL)
        │                 │     │           │                       ├── <HexAddressInput>
        │                 │     │           │                       ├── <RenderAssetPreview>
        │                 │     │           │                       ├── <ErrorAssetPreview>
        │                 │     │           │                       └── <DataListSelect>
        │                 │     │           │                             └── onSelect → setInputState(CLOSE_SELECT_PANEL)
        │                 │     │           │                                   └── callback → setSellTokenContract()
        │                 │     │           ├── Balance: {formattedBalance}
        │                 │     │           ├── {buySellText}
        │                 │     │           └── <ManageSponsorsButton /> ← if isSpCoin
        │                 │     ├── <BuySellSwapArrowButton>
        │                 │     └── <AssetSelectProvider> (BUY)
        │                 │           └── <BaseSelectPanel> (BUY)
        │                 │                 ├── <input /> ← user edits amount
        │                 │                 ├── <TokenSelectDropDown>
        │                 │                 │     └── onClick → setSpCoinDisplay(TOKEN_LIST_SELECT_PANEL)
        │                 │                 ├── isContainerVisible → <TokenSelectScrollPanel>
        │                 │                 │     └── <TokenListSelectPanel>
        │                 │                 │           └── <AssetListSelectPanel>
        │                 │                 │                 └── <AddressSelect> (BUY)
        │                 │                 │                       ├── <HexAddressInput>
        │                 │                 │                       ├── <RenderAssetPreview>
        │                 │                 │                       ├── <ErrorAssetPreview>
        │                 │                 │                       └── <DataListSelect>
        │                 │                 │                             └── onSelect → setInputState(CLOSE_SELECT_PANEL)
        │                 │                 │                                   └── callback → setBuyTokenContract()
        │                 │                 ├── Balance: {formattedBalance}
        │                 │                 ├── {buySellText}
        │                 │                 └── <AddSponsorshipButton /> ← if isSpCoin
        │                 │                       ├── onClick → setSpCoinDisplay(RECIPIENT_LIST_SELECT_PANEL)
        │                 │                       └── isContainerVisible → <RecipientSelectScrollPanel>
        │                 │                             └── <RecipientListSelectPanel>
        │                 │                                   └── <AddressSelect> (Recipient)
        │                 │                                         ├── <HexAddressInput>
        │                 │                                         ├── <RenderAssetPreview>
        │                 │                                         ├── <ErrorAssetPreview>
        │                 │                                         └── <DataListSelect>
        │                 │                                               └── onSelect(account, state)
        │                 │                                                     └── if state === CLOSE_SELECT_PANEL:
        │                 │                                                           ├── callbackAccount(account)
        │                 │                                                           ├── hasErroredRef = false
        │                 │                                                           └── setSpCoinDisplay(SHOW_RECIPIENT_CONTAINER)
        ├── <ConnectTradeButton>
        ├── <AffiliateFee>
        └── <FeeDisclosure>
