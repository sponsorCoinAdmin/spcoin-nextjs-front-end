This workspace keeps local SponsorCoin npm package sources and tarball backups together.

Expected subdirectories:

- `spCoinAccess/spCoinNpmSource/packages` for editable local package sources
- `spCoinAccess/spCoinNpmSource/backups` for packed npm tarballs and backup artifacts
