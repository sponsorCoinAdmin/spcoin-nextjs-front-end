import { OX_API_KEY, FEE_WALLET_DETAILS } from '../../apiConfig'
const networkName="Ethereum"
const networkURL = "https://api.0x.org";
const chainId = 1

export { OX_API_KEY, chainId, networkName, networkURL, FEE_WALLET_DETAILS }