import { getURLParams, OX_API_KEY, FEE_WALLET_DETAILS } from '../apiConfig'
const networkName="Polygon"
const networkURL = "https://polygon.api.0x.org";
const chainId = 137

export { getURLParams, OX_API_KEY, chainId, networkName, networkURL, FEE_WALLET_DETAILS }