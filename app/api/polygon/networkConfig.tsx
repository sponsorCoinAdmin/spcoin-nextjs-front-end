import { getURLParams, OX_API_KEY, feeWalletDetails } from '../apiConfig'
const networkName="Polygon"
const networkURL = "https://polygon.api.0x.org";
const chainId = 137

export { getURLParams, OX_API_KEY, chainId, networkName, networkURL, feeWalletDetails }