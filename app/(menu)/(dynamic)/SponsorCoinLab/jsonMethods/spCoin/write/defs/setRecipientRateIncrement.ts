import type { MethodDef } from '../../../shared/types';

export const methodDef: MethodDef = {
  title: 'setRecipientRateIncrement',
  params: [
    { label: 'New Recipient Rate Increment', placeholder: 'uint recipient rate increment', type: 'uint' },
  ],
};

export default methodDef;
