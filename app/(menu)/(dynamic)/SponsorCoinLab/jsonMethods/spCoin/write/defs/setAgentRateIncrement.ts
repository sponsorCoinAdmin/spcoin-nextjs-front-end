import type { MethodDef } from '../../../shared/types';

export const methodDef: MethodDef = {
  title: 'setAgentRateIncrement',
  params: [{ label: 'New Agent Rate Increment', placeholder: 'uint agent rate increment', type: 'uint' }],
};

export default methodDef;
