import type { MethodDef } from '../../../shared/types';

export const methodDef: MethodDef = {
  title: 'setUpperAgentRate',
  params: [{ label: 'New Upper Agent Rate', placeholder: 'uint upper agent rate', type: 'uint' }],
};

export default methodDef;
